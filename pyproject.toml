[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = [
    "E4",
    "E7",
    "E9",
    "F",
    "I",
]
[tool.pytest.ini_options]
testpaths = ["tests"]
# The `hass` fixture from pytest-homeassistant-custom-component is async.
asyncio_mode = "auto"
